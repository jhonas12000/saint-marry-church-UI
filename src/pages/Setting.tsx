const Settings = () => {
  return (
    <div>
      <h2 className="text-xl font-bold mb-4">Settings</h2>
      <p className="text-gray-700 mb-2">Future features like password reset, profile update, and notification preferences can be managed here.</p>
    </div>
  );
};

export default Settings;
